# MySQL 容器自身初始化用
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=web
MYSQL_USER=web
MYSQL_PASSWORD=

# 后端连接 MySQL 用(容器内运行时 DB_HOST=mysql)
DB_HOST=mysql
DB_PORT=3306
DB_NAME=web
DB_USER=web
DB_PASSWORD=

# 后端监听端口
PORT=3000

# 管理提交文章的 Bearer token(仅 server 用,必填,真实值只放服务器 .env)
ADMIN_TOKEN=
